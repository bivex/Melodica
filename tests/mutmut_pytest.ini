[pytest]
# Minimal config for mutmut — no qt, no heavy plugins
addopts = -x -q --tb=no --no-header -p no:qt -p no:anyio -p no:cov
testpaths = tests
python_files = test_harmonize.py test_harmonize_mutations.py
